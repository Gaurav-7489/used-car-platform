'use client';
import {FormEvent,useState} from 'react';
export default function LeadForm({carTitle}:{carTitle:string}){const [sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSent(true)}if(sent)return <div className="notice" style={{background:'#ecfdf3',borderColor:'#bbf7d0',color:'#166534'}}><strong>Lead captured.</strong><p style={{marginBottom:0}}>Demo lead saved to the MVP flow. Connect /api/leads + Supabase/CRM for production.</p></div>;return <form onSubmit={submit} style={{display:'grid',gap:10}}><input className="input" name="name" required placeholder="Your name"/><input className="input" name="phone" required placeholder="Phone number" inputMode="tel"/><input className="input" name="budget" placeholder="Budget (optional)"/><textarea className="input" name="message" placeholder={`I'm interested in the ${carTitle}`} rows={4}/><button className="btn btn-primary" type="submit">Request callback</button><a className="btn btn-brand" href={`https://wa.me/?text=${encodeURIComponent(`Hi VandLabs Motors, I'm interested in the ${carTitle}`)}`}>WhatsApp placeholder</a></form>}
