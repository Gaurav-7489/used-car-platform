'use client';
import {useState} from 'react'; import {cars,formatINR} from '@/lib/cars'; import {Search, Plus, MoreHorizontal} from 'lucide-react';
export default function Inventory(){const [q,setQ]=useState('');const filtered=cars.filter(c=>c.name.toLowerCase().includes(q.toLowerCase()));return <><div className="topbar"><div><p className="eyebrow">OPERATIONS</p><h1 className="page-title">Inventory</h1><p className="muted">Manage vehicles that appear across your digital storefront.</p></div><button className="btn btn-brand"><Plus size={15}/> Add vehicle</button></div><div className="panel"><div className="toolbar"><div style={{position:'relative'}}><Search size={15} style={{position:'absolute',left:12,top:12,color:'#98a2b3'}}/><input className="input" style={{paddingLeft:35}} placeholder="Search vehicles..." value={q} onChange={e=>setQ(e.target.value)}/></div><select className="select"><option>All status</option><option>Available</option><option>Reserved</option><option>Sold</option></select><select className="select"><option>All fuel types</option><option>Petrol</option><option>Diesel</option></select></div><div className="table-wrap"><table className="table"><thead><tr><th>Vehicle</th><th>Price</th><th>Specs</th><th>Location</th><th>Status</th><th>Actions</th></tr></thead><tbody>{filtered.map(c=><tr key={c.id}><td><strong>{c.name}</strong><br/><span className="muted">{c.id.toUpperCase()} · {c.year}</span></td><td><strong>{formatINR(c.price)}</strong></td><td>{c.km.toLocaleString('en-IN')} km · {c.fuel}<br/>{c.transmission}</td><td>{c.city}</td><td><span className={'status '+(c.status==='Available'?'qualified':c.status==='Reserved'?'appointment':'lost')}>{c.status}</span></td><td><button className="btn btn-light"><MoreHorizontal size={16}/></button></td></tr>)}</tbody></table></div></div></>}
