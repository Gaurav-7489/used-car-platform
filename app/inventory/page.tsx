import Link from "next/link";
import { cars, formatPrice } from "@/lib/cars";

export default function InventoryPage(){return <><header className="topbar"><div className="container nav"><div className="brand">VANDLABS MOTORS</div><nav className="navlinks"><Link href="/">Home</Link><Link href="/dashboard">Dashboard</Link></nav></div></header><main className="section"><div className="container"><h1>Inventory</h1><p className="muted">Browse the current dealership stock. Filters are visual MVP controls for now.</p><div className="filters"><input className="input" placeholder="Search make, model, location..."/><select className="input"><option>Any fuel</option><option>Petrol</option><option>Diesel</option></select><select className="input"><option>Any transmission</option><option>Automatic</option><option>Manual</option></select><select className="input"><option>Any status</option><option>Available</option><option>Sold</option></select></div><div className="grid">{cars.map(car=><article className="card" key={car.id}><div className="carimg">IMAGE PLACEHOLDER</div><div className="cardbody"><span className="pill">{car.status}</span><h3>{car.title}</h3><div className="price">{formatPrice(car.price)}</div><div className="meta"><span>{car.year}</span><span>{car.km.toLocaleString("en-IN")} km</span><span>{car.fuel}</span><span>{car.transmission}</span></div><div className="cardactions"><Link className="btn primary" href={`/cars/${car.id}`}>View details</Link><a className="btn secondary" href={`https://wa.me/?text=Interested%20in%20${encodeURIComponent(car.title)}`}>WhatsApp</a></div></div></article>)}</div></div></main></>}
