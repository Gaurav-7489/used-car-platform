import Link from 'next/link';
import { ArrowRight, MessageSquare, BarChart3, ShieldCheck } from 'lucide-react';
import { cars, formatINR } from '@/lib/cars';
export default function Home(){const available=cars.filter(c=>c.status==='Available');return <><header className="public-nav"><Link className="logo" href="/">VAND<span>LABS</span> MOTORS</Link><nav className="navlinks"><Link href="/cars">Inventory</Link><Link href="/dashboard">Dealer Dashboard</Link><Link href="#how">How it works</Link></nav><Link className="btn btn-brand" href="/dashboard">Dealer login</Link></header><main><section className="hero"><div className="container hero-grid"><div><span className="eyebrow">PRE-OWNED · VERIFIED INVENTORY</span><h1>Find a car.<br/>Start a conversation.</h1><p>Browse our live-style inventory, inspect the details, request a test drive and connect with the dealership in seconds.</p><div className="hero-actions"><Link className="btn btn-brand" href="/cars">Browse inventory <ArrowRight size={15}/></Link><a className="btn btn-light" href="#how">How it works</a></div></div><div className="hero-card"><h3>Dealership pulse</h3><div className="metric-row"><span>Vehicles available</span><strong>{available.length}</strong></div><div className="metric-row"><span>Customer response</span><strong className="green">Fast</strong></div><div className="metric-row"><span>Test-drive requests</span><strong>12 this week</strong></div><div className="metric-row"><span>Lead channels</span><strong>Google · Social · WhatsApp</strong></div></div></div></section><section className="section"><div className="container"><div className="section-head"><div><h2>Featured inventory</h2><p className="muted">Demo inventory — ready to connect to a real database.</p></div><Link href="/cars" className="btn btn-light">View all</Link></div><div className="cars-grid">{available.slice(0,3).map(c=><article className="car-card" key={c.id}><div className="car-img">VEHICLE IMAGE</div><div className="car-body"><span className="tag">{c.status}</span><h3 className="car-title">{c.name}</h3><div className="muted">{c.year} · {c.city}</div><div className="specs"><span>{c.km.toLocaleString('en-IN')} km</span><span>{c.fuel}</span><span>{c.transmission}</span></div><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span className="price">{formatINR(c.price)}</span><Link className="btn btn-primary" href={`/cars/${c.id}`}>View car</Link></div></div></article>)}</div></div></section><section id="how" className="section" style={{paddingTop:10}}><div className="container"><div className="section-head"><div><h2>One connected journey</h2><p className="muted">The storefront is only the front door. Every enquiry can flow into the dealership workspace.</p></div></div><div className="cars-grid"><div className="panel"><MessageSquare size={22}/><h3>1. Enquire</h3><p className="muted">WhatsApp, call, enquiry and test-drive actions carry vehicle context.</p></div><div className="panel"><ShieldCheck size={22}/><h3>2. Follow up</h3><p className="muted">Leads enter a structured pipeline with qualification and appointment stages.</p></div><div className="panel"><BarChart3 size={22}/><h3>3. Learn</h3><p className="muted">Channel, vehicle and funnel signals become visible in the dealer dashboard.</p></div></div></div></section></main><footer className="footer"><div className="container"><strong>VandLabs Motors</strong><p>Demo dealership · WhatsApp, database and AI integrations are placeholders in this MVP.</p></div></footer></>}
