import Link from 'next/link'; import {cars,formatINR} from '@/lib/cars';
export default function Cars(){const available=cars.filter(c=>c.status!=='Sold');return <><header className="public-nav"><Link className="logo" href="/">VAND<span>LABS</span> MOTORS</Link><nav className="navlinks"><Link href="/">Home</Link><Link href="/dashboard">Dealer Dashboard</Link></nav><Link className="btn btn-brand" href="/dashboard">Dealer login</Link></header><main className="section"><div className="container"><div className="section-head"><div><p className="eyebrow">INVENTORY</p><h1 className="page-title">Available cars</h1><p className="muted">Browse {available.length} demo vehicles. Connect your real inventory source when ready.</p></div></div><div className="cars-grid">{available.map(c=><article className="car-card" key={c.id}><div className="car-img">VEHICLE IMAGE</div><div className="car-body"><span className="tag">{c.status}</span><h3 className="car-title">{c.name}</h3><div className="muted">{c.year} · {c.city}</div><div className="specs"><span>{c.km.toLocaleString('en-IN')} km</span><span>{c.fuel}</span><span>{c.transmission}</span></div><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span className="price">{formatINR(c.price)}</span><Link className="btn btn-primary" href={`/cars/${c.id}`}>Details</Link></div></div></article>)}</div></div></main></>}
